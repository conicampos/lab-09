<% if object.errors.any? %>
  <div class="alert alert-danger shadow-sm">
    <h5 class="alert-heading"><%= pluralize(object.errors.count, "error") %> prohibited this record from being saved:</h5>
    <ul class="mb-0">
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>